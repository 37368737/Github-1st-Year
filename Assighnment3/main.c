#include <stdio.h>
#include <stdlib.h>

int main()
{
   int num,count,i,col,row;
    printf("Size of the odd number multiplication table: ");
    scanf("%d", &num);
    printf("\n");

    for(){
        for()


        printf("%d\t%d",)
    }
}
